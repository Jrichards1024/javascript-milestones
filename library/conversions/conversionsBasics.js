function conversionsBasics(){
  value = 12
  console.log(`now value equals a number: ${value}`)
  console.log(`in order to change it to a string you use the toString function: '${value.toString()}'`)
  console.log(`to change the value back to a number you can use number(): ${Number(value)}`)
}
conversionsBasics()
